# Five-minute demo script

1. Open landing page → **Start Demo** → sign in as `admin@closerai.demo` / `DemoPass123!`.
2. Show Overview metrics and objection chart.
3. Open **Leads** → Sarah Chen (BrightLedger) → review pain point and status.
4. Open **Demo Lab** → select Sarah → simulate `call_started`, then `call_ended`, then `call_analyzed`.
5. Land on Call Details — show transcript, score ~87, BANT cards, price objection, Zapier mention, next action.
6. Open **Appointments** and **Analytics** to show meeting conversion.
7. Explain that a real **Start AI Call** invokes Retell only when API credentials are configured — never silently faked.
