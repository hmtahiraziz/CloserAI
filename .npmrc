# Hoisted layout so PaaS / serverless tracers can resolve deps like express.
# pnpm's default isolated linker breaks many deploy packagers (Vercel/Lambda NFT).
node-linker=hoisted
public-hoist-pattern[]=*express*
public-hoist-pattern[]=*body-parser*
